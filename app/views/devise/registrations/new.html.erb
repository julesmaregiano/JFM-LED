<div class="full-screen-wrapper-with-navbar background-login flexbox">
  <div class="sign-up-box">
    <div class="login-top">
      <%= render "devise/shared/links" %>
    </div>
    <div class="flexbox text-center">
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= f.error_notification %>
            <div class="row">
              <div class="col-xs-6 form-inputs">
                <%= controller_name %>
                <h4><strong>Vos coordonnées</strong></h4>
                <%= f.input :email, required: true, autofocus: true %>
                <%= f.input :first_name, label: "Prénom", require: true %>
                <%= f.input :last_name, label: "Nom", required: true %>
                <%= f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
                <%= f.input :password_confirmation, required: true %>
              </div>
              <div class="col-xs-6 form-inputs">
              <h4><strong>Votre Entreprise</strong></h4>
                <%= f.input :company %>
              </div>
            </div>
            <div class="form-actions">
              <%= f.button :submit, "Sign up" %>
            </div>
          <% end %>


    </div>
  </div>
</div>

