<div class="dashboard-wrap">
  <%= render "/shared/sidebar"%>

  <div class="dashboard-content">
    <div class="padding-box">
      <div class="row">
        <%= render "shared/week_calendar", availabilities: @availabilities, techs: @techs %>
      </div>
      <div class="row">
        <h4>Bookings à valider</h4>
        <% @bookings.each do |booking| %>
          <div class="col-xs-3 card small-card">
            <%= render "shared/small_booking_card", booking: booking %>
            <div class="text-center">
              <%= simple_form_for [@user.role, booking] do |f| %>
                <div class="availabilities-fields"></div>
                <%= f.button :submit %>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
